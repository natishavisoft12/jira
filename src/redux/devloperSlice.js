import { createSlice } from "@reduxjs/toolkit"

const initialState ={
    listofdevlopers:[]
}
const devloperSlice = createSlice({
    name:"devloper",
    initialState,
    reducers:{
        

    }
})